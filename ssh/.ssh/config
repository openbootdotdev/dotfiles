# SSH Configuration
# Add your hosts below

# Example GitHub configuration
# Host github.com
#     HostName github.com
#     User git
#     IdentityFile ~/.ssh/id_ed25519
#     AddKeysToAgent yes

# Example server configuration
# Host myserver
#     HostName server.example.com
#     User deploy
#     IdentityFile ~/.ssh/id_ed25519
#     Port 22

# Default settings for all hosts
Host *
    AddKeysToAgent yes
    # IdentitiesOnly yes — uncomment after configuring IdentityFile per host
    # (prevents SSH from trying all keys, but blocks hosts without explicit config)
    ServerAliveInterval 60
    ServerAliveCountMax 3

    # macOS Keychain integration (uncomment on macOS)
    # UseKeychain yes
